# backend/.env.local (backend en host)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/bookapp

# backend/.env.docker (backend en contenedor)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/bookapp
